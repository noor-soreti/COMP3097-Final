/*
1. Declare tables and queries in sql in drift
2. Generate Dart code to run them (test_database.dart)
*/ 

CREATE TABLE test (
  id INT NOT NULL PRIMARY KEY AUTOINCREMENT,
  name TEXT,
  description TEXT
)

// Query example - query name then colon (:) then SQL
testQuery: SELECT * FROM test;

